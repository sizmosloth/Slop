# The Main Cause of Halucinations even on small commands

**memory.json**

The llm is literally reading every new prompt and then goes through the memory.json to check for previous answers for new output. But the previous answers were all lies generated by the llm by it self and now its just believing that all the previous messages are sources of new information and treat them as RAG so it always halucinate on generating new prompts because old messages are false.

Small models were trained on less parameters so they can't work lke big models like gpt and sonnet too and they can't properly train on more dataset. 

**system prompts**

The pre prompts inserted in the model to work upon also did'nt work. So Refining system prompts again in proper structure and defined way so the model can think properly and keep detailed view on it.

**fixes**

```
- Fixing System Prompts.
- Trying more ways to help the model to remember past conversations or the improtant things in the prompt.
```

